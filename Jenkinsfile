/* Requires the Docker Pipeline plugin */
pipeline {
    stages {
        stage('build') {
            steps {
                sh 'java -version'
                sh 'echo I made a second step!'
            }
        }
    }
}